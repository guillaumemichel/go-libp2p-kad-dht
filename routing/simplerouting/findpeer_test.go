package simplerouting

import "testing"

// test find peer with connected peer
func TestFindPeerConnected(t *testing.T) {
	// create a new simple routing
	// create a new peer
	// add peer to peerstore
	// connect peer to simple routing
	// find peer
	// assert peer found
}

// test find peer with peer in peerstore

// test find peer with cannot connect peer

// test find peer with peer not in peerstore

// test find peer with non existent peer

// test find peer with peer that just went offline (still in other peer's peerstore)
